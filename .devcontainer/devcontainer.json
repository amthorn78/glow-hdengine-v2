{
  "name": "Glow HD Engine Codespace",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "remoteEnv": {
    "LC_ALL": "C",
    "LANG": "C",
    "TZ": "UTC",
    "PATH": "/home/vscode/.local/bin:${containerEnv:PATH}",
    "OPENAI_API_KEY": "${localEnv:OPENAI_API_KEY}"
  },
  "containerEnv": {
    "DEV_SAMPLER_URL": "http://127.0.0.1:8000/internal/dev/sampler"
  },
  "postCreateCommand": "python -m pip install --upgrade pip && python -m pip install --user -e . && python -m engine.cli --help>/tmp/devcontainer_cli_help.log && curl -fsSL https://fnm.vercel.app/install | bash -s -- --install-dir \"$HOME/.fnm\" --skip-shell && export PATH=\"$HOME/.fnm:$PATH\" && eval \"$(fnm env)\" && fnm install --lts && npm install -g @openai/codex && echo 'export PATH=\"$HOME/.fnm:$PATH\"' >> ~/.bashrc && echo 'eval \"$(fnm env)\"' >> ~/.bashrc",
  "customizations": {
    "vscode": {
      "settings": {}
    }
  }
}
