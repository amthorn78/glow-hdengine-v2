# HD Engine Repo Docs — Index (EPIC024 aligned)

## Acceptance & evidence
- Evidence skeleton (same-PR rule): `docs/evidence/INDEX.json`, `docs/evidence/INDEX.sha256`, and `artifacts/evidence_index.jsonl` (all with `.path_proof.txt` siblings; mirror self-records include `mirror_body_sha256`). Refresh with `python tools/evidence/update_evidence_index.py` → `python tools/evidence/orientation_demo.py` → their `--check` variants → `ci/checks/check_mirror_schema.sh`.
- EPIC024 acceptance and QA ledger: acceptance map `docs/acceptance_map_epic024.json`, token matrix `audit/qa/hde-epic024/token_evidence_matrix.md`, viability log `audit/qa/hde-epic024/acceptance_map_viability.log`, QA step manifest `audit/qa/hde-epic024/qa_step_logs_manifest.json`, and per-check logs under `audit/qa/hde-epic024/checks/<check_id>/primary.log`. Doc deltas live at `audit/docdeltas/hde-epic024_doc_deltas.md` and `audit/qa/hde-epic024/00_meta/doc_deltas.md`. Close-pack: `audit/EPIC-024_MANIFEST.json`, `audit/EPIC-024_close_report.md`.
- Canonical JSON gate home: `python tools/evidence/run_canonical_json_gate.py` (CI step “Run canonical JSON gate (closed rails)”) writes gate artifacts under `audit/gates/json_gate/canonical/` (`json_gate_check_log.ndjson`, `json_gate_compare_log.ndjson`, `json_gate_structured_record.json`, each with `.path_proof.txt` siblings). The legacy catalog check report remains at `audit/gates/canonical_json/json_canonical_check.log`.
- Evidence index snapshot gate: `python tools/evidence/generate_evidence_index_snapshot.py` writes `audit/gates/evidence_index_snapshot/evidence_index_snapshot.json` plus `.path_proof.txt`; validation coverage is in `tests/evidence/test_evidence_index_snapshot.py`.
- EPIC022 evidence: showcompat D2 captures (`artifacts/cli/showcompat/stdout.json`, `.sha256`, `args.json` generated by `tools/cli/generate_showcompat_artifacts.py`), env-pin and refusal parity evidence (`audit/gates/determinism/env_pins.log`, `parity/errors_reader_cli.db_unavailable.*`, `parity/errors_reader_cli.vendor_attempt_closed_rails.*`), `/internal/version` D3 bundle (`artifacts/ops/internal_version/{body_get.json,body_get.sha256,headers_get.txt,headers_head.txt,cond_if_none_match_headers.txt,cond_if_modified_since_headers.txt,two_run_identity.log}` coupling log), acceptance map (`docs/acceptance_map_epic022.json`), token evidence matrix (`audit/qa/hde-epic022/token_evidence_matrix.md`), and finalized close-pack artifacts (`audit/EPIC-022_MANIFEST.json`, `audit/EPIC-022_close_report.md`, `artifacts/sanity/sanity.log` + `.path_proof.txt`). Known canon mismatch: PF20 references a D3 provenance note while implementation follows the PF10 posture via the governed two-run/coupling log.
- EPIC022 Remediation 1 (Release identity): Freeze-Pack SoT is the checked-in `catalog/manifest.json` (keys: `root`, `version`, `built_at_utc`, `files`, no self-listing); canonical bytes rule applies (UTF-8, ASCII-sorted keys, compact separators, one trailing `\n`) and `release_id = sha256(canonical_bytes(catalog/manifest.json))`. Evidence copy lives at `artifacts/math/freeze_pack_manifest.json` (byte-identical to the SoT); `manifest_snapshot.json` is evidence-only. Governed identity evidence set: `artifacts/math/release_id.txt`, `artifacts/math/release_id_recompute.log`, `artifacts/math/checksums_audit.log`, `artifacts/math/manifest_snapshot.json`, `artifacts/proofs/env_pins.txt`. Validation commands (closed rails; Python entrypoints in CI): `python scripts/release_id_recompute.py --check`, `python ci/checks/check_release_identity.sh`, and `python tools/evidence/run_sanity_pipeline.py` (includes the gate). `--check` writes the recompute log/sha sidecar; run in a clean workspace or discard local changes.
- EPIC021 evidence: registry report (`artifacts/registry/registry_report.json`), sanity pipeline logs (`artifacts/sanity/sanity.log` + `.path_proof.txt`), Evidence Index/Mirror updates (`docs/evidence/INDEX.json`, `docs/evidence/INDEX.sha256`, `artifacts/evidence_index.jsonl`), QA_ROOT logs (`audit/qa/hde-epic021/`), acceptance map (`docs/acceptance_map_epic021.json`), and token evidence matrix (`audit/qa/hde-epic021/token_evidence_matrix.md`).
- EPIC020 separation: acceptance map bindings cover `ERROR_*`, `PRESENTER_*`, and `/internal/version` artifact families tied to canonical error envelopes, shared presenter/emitter, and identity artifacts. QA checklist: `docs/QA_CHECKLIST_EPIC020.md`.
- EPIC019 close-out: sampler/core acceptance map (`docs/acceptance_map_epic019.json` + path proof) and consolidated acceptance map roster (`docs/acceptance_maps.json`) capture tokens for HDE-DISS003/004; entries are governed and indexed.
- EPIC018 references remain for Calcination: manifest (`audit/EPIC-018_MANIFEST.json`), close report (`audit/EPIC-018_close_report.md`), and config acceptance map (`audit/EPIC-018_config_acceptance_map.json`). All are governed with path proofs and mirrored in PF-Canon (see PF20 — Phased Epics, PF19 — QA Guide).
- Evidence Index (human): `docs/evidence/INDEX.json`
- Evidence Index (machine): `artifacts/evidence_index.jsonl` (self-record path proof includes `mirror_body_sha256`)
- Registry report generator: `tools/generate_registry_report.py`
- Orientation demo and sanity pipeline: `tools/evidence/orientation_demo.py`, `tools/evidence/run_sanity_pipeline.py` (closed rails enforced by the determinism helper).
- Evidence index updater: `tools/evidence/update_evidence_index.py` (refreshes Index/Mirror, including registry_report, sanity, and showcompat entries); mirror schema check: `ci/checks/check_mirror_schema.sh` (validates self-record/mirror path proofs).
- Sampler evidence generator: `tools/evidence/generate_sampler_evidence.py` (dev sampler CLI + HTTP harness runs, seed replay/diversity/ABBA/two-run logs).
- Engine Core evidence generator: `tools/evidence/generate_engine_core_evidence.py` (purity/ABBA/two-run/JSON compare logs).
- QA harnesses: EPIC021 harness `tools/qa/epic021_qa.py` (QA_ROOT logs, acceptance-map viability); legacy dev sampler healthchecks (`scripts/qa/dev_sampler_healthcheck.py`, `scripts/qa/dev_sampler_live_qa.py`) and D6 vendor Live QA (`scripts/qa/d6_live_vendor_qa.py`).
- Env pins gate: `ci/checks/check_env_pins.sh` validates `LC_ALL=C LANG=C TZ=UTC SAFE_MODE=1 ALLOW_NETWORK=0` for docs/tests; closed rails apply to CLI and harnesses.
- CLI guard rails: `tools/cli/serializer_grep_guard.py`, `tools/cli/emitter_symbol_proof.py` (outputs under `artifacts/cli/guards/`).
- Error/presenter/identity evidence families: `errors/`, `parity/`, `artifacts/presenter/`, `artifacts/ops/internal_version/`, and `artifacts/math/` are mirrored in Index/Mirror.
- Config governance: `tools/config/generate_config_artifacts.py`, `tools/config/generate_bundles.py`, and schemas in `docs/schemas/`.
- Past epics remain referenced through PF-Canon titles (PF12 — Schemas & Artifacts, PF14 — Mechanics, PF05 — CLI/API/Vendor Ref).

## Local map
- **ARCHITECTURE.md** — entrypoints and providers
- Architecture snapshots: `_arch/<epic_id>_<timestamp>/` (routes/imports/tree)
- EPIC019 remedial summary: `docs/hde_epic019_remediation.md` (dev Reader helper, DEV_SAMPLER_URL, dev sampler harnesses)

## Developer quick-links
- Emitter: `engine/presenter/emitter.py::emit_compact_json`
- Serializer: `engine/serializer/canon.py::dumps` (UTF-8; sort_keys; one LF)
- Compat handler: `engine/http/compat_handler.py` (APP_ENV gating)
- Dev sampler endpoint: `adapter/http_reader.py::/internal/dev/sampler` (APP_ENV=dev; helper `scripts/dev_start_reader.sh`)
- Dev sampler CLI command: `engine/cli/main.py::dev:sampler`; harnesses under `scripts/qa/`
- Engine Core compute: `engine/core/core.py::compute_core`
- Determinism helper: `engine/runtime/determinism_env.py`

## Evidence posture crib
- Use closed rails: `LC_ALL=C LANG=C TZ=UTC SAFE_MODE=1 ALLOW_NETWORK=0`.
- Update indexes with `tools/evidence/update_evidence_index.py`; run orientation demo and sanity pipeline (includes sampler/core runs) to validate mirror/path-proof discipline.
- Regenerate sampler/core evidence via their generators before updating indexes; guard proofs must be regenerated when CLI or sampler surfaces change.

## Config & bundles (D5/D6)
- Governed configs live in `config/` (Magic-10 bands, toggles). Regenerate via `tools/config/generate_config_artifacts.py` and index with PF12 discipline.
- Typed FE/BE bundles: generated by `tools/config/generate_bundles.py` using schemas `docs/schemas/config_bundle_fe.json` and `docs/schemas/config_bundle_be.json`.
- Acceptance mapping: `audit/EPIC-018_config_acceptance_map.json` connects PF09 tasks → artifacts → tokens/tests (Calcination). EPIC019 sampler/core acceptance lives in `docs/acceptance_map_epic019.json` with path proofs.

## Epic references
- Transport and caching rules: PF05 — CLI/API/Vendor Ref and PF12 — Schemas & Artifacts (title references only; details live in PF-Canon).
- Epic process and QA posture: PF06 — Epic Process, PF19 — QA Guide.
