"""Packaged release-manifest resources.

The catalog manifest remains the single checked-in release identity source.
Making the catalog a data package lets installed builds derive the same
identity without copying the manifest into generated source.
"""
