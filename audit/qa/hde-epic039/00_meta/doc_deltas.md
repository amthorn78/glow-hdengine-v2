# HDE-EPIC039 Doc Deltas

## Governed surfaces

- Consolidated the current D1 governed-JSON target inventory and its validator, schema, and declared-set bindings in the existing canonical JSON gate. (PF12 — Schemas & Artifacts, §4, §6.4, §8.1, §8.2; PF14 — Mechanics Guide, §4, §5.)
- Added deterministic schema-declared arrays-as-sets reporting and promoted its proof through the Human Evidence Index and Machine Mirror. (PF04 — Governance, §2.0.6; PF12 — Schemas & Artifacts, §6.4, §8.2.)
- Reconciled the frozen-input manifest while preserving release version and build timestamp, retained the frozen checked-in historical release evidence, and kept current release-bound derivatives external to the source tree. (PF12 — Schemas & Artifacts, §5, §6.4.)
- Migrated the sealed narrative pack from the retired 240-row `personal|shared` encoding to PF17's complete 360-row `shared|a_to_b|b_to_a` candidate grid, preserving PF18 source ids and copy, candidate-blocking only the two PF15-invalid source paragraphs, admitting the governed ASCII hyphen while retaining the em-dash prohibition, and binding pack identity to exact LF-terminated bytes. (PF15 — Copy Tonality Guide, §3; PF17 — Narratives Guide, §2.3, §3.3, §3.7, §4.1; PF12 — Schemas & Artifacts, §2.8, §4.)
- Removed the retained HDE-EPIC032 narrative writers from the current external-attestation closure and converted their entry points to frozen-byte verification, so the direction-native runtime cannot overwrite capture-time router or registry evidence. (PF12 — Schemas & Artifacts, §5, §6.4.)

## Scope boundaries

This implementation evidence makes no claim of token satisfaction, QA PASS, acceptance, PF09 status movement, OPS completion, deployment, epic closeout, or complete PF17 composer and transport conformance. It changes the sealed narrative registry and direction-specific runtime selection only; it does not add public routes, change compatibility payload contracts, alter Human Design calculations, meanings, scoring, or bands, or reinterpret non-narrative ordered arrays.

## PR-02 evidence-ledger publication

- Consolidated the Human Index, both checksums, Machine Mirror self-record, path proofs, and orientation into one updater-owned staged transaction with immediate fixed-point validation; orientation write compatibility now delegates to that sole writer. (PF04 — HDE Governance, §2.0.6; PF12 — HDE Schemas and Artifacts, §8.3; PF14 — HDE Mechanics Guide, §§1.3.1–1.3.2.)
- Replaced active repair-style updater/orientation/updater sequences with one updater publication followed by read-only checks, and extended the existing hash and final-LF controls across the complete D2 ledger skeleton. (PF05 — HDE CLI/API/Vendor Ref, §0.4; PF10 — HDE Build Notes, Addenda 2.3–2.5; PF14 — HDE Mechanics Guide, §1.3.)

These PR-02 changes do not claim QA PASS, acceptance, token satisfaction, PF09 status movement, OPS completion, deployment, or epic closeout.

## PR-03 reusable current-state QA harness

- Hardened the generic QA harness around canonical epic-derived paths, stable check identities, the closed five-status model, same-interpreter pytest execution, PF27-v2 atomic primary logs, deterministic current-state manifests, and causally classified acceptance-map viability with PF04 token-roster and evidence-reference validation. (PF04 — HDE Governance, §2.0; PF14 — HDE Mechanics Guide, §§1.6.1–1.6.3; PF19 — Glow QA Guide, §§2.2.5, 4.4.5; PF27 — Plan Templates, §Step-log header schema expectations (required; v2).)
- Requalified the active EPIC021 executable and close-pack family through the generic check-centric harness, including current receipts, manifest, viability, acceptance bindings, Doc-Delta pair, and evidence-graph companions; the current EPIC027, EPIC028, and EPIC029 generator, close-manifest, viability, and related governed-artifact families were also requalified. Historical run evidence remains immutable and non-gating. (PF06 — Epic Process Guide, §0.5; PF14 — HDE Mechanics Guide, §1.6.3; PF19 — Glow QA Guide, §§2.2.5, 4.4.5; PF27 — Plan Templates, §Executable helper boundary; PF10 — HDE Build Notes, §§2.7–2.8.)

These PR-03 changes do not claim `QA_HARNESS_DISCIPLINE_OK`, `QA_HARNESS_ENTRYPOINT_SELFTEST_OK`, any other token satisfaction, QA PASS, acceptance, PF09 status movement, OPS or Live QA execution, deployment, or epic closeout. They do not create HDE-EPIC039 QA verdict artifacts or change public behavior.

## PR-04 Calcination Pass 6 CI remediation

- Reclassified the complete execution-ref CI graph by continuing product and delivery risk, removed the withdrawn HDE-EPIC038 DEV-01/DEV-02 closeout, token-matrix, private-receipt, authenticated-consumption, and source-feedback machinery, and retired only its current governed registrations while preserving HDE-EPIC038 implementation history, QA history, failure evidence, nonclaims, and immutable historical artifacts. The canonical release manifest remains the sole tracked release-identity source and is validated directly by release mechanics rather than indexed as evidence; its pre-existing proof companion remains untouched as historical capture material so manifest-only cuts remain one-file changes. (PF10 — HDE Build Notes, §§2.3, 2.8; PF12 — HDE Schemas and Artifacts, §6.2.1.)
- Replaced overlapping all-branch push and pull-request eligibility with one exact-head pull-request boundary plus default-branch push protection, automatic superseded-PR cancellation, a repository-owned fail-closed change classifier, one shared dependency setup, affected-surface validation lanes, and one stable truthful `test` conclusion. Retired the self-referential EPIC020 bundle/index build from active CI while preserving its frozen captures; the generic index, mirror, proof, hash, path, and orientation checks remain read-only. Current Human Index membership automatically adds the evidence lane before documentation shortcuts, so governed primaries cannot bypass proof/Index/Mirror checks while ungoverned documentation remains light. Documentation-only and historical-record-only changes avoid unrelated heavy validation; workflow, dependency, or classifier changes select every established safe lane, while unclassified change surfaces fail closed instead of reporting green. (PF10 — HDE Build Notes, §§2.5, 2.8.)
- Narrowed the existing release-attestation and sanity mechanics to a fifteen-stage generic chain for their continuing exact-source, clean-tree, closed-rails, canonical-byte, secret-safety, mapped-cache, database, runtime, and release risks; removed HDE-EPIC038 administrative identity from internal pipeline and gating mechanics while retaining PF12's `PR06R_B_FINAL_PASS` v1 wire literal and complete nonclaim roster solely for compatibility. The former nineteen-stage HDE-EPIC038 administrative and OPS stages, including frozen OPS evidence, remain in their historical/audit boundary. The reusable current-state QA subsystem accepted through PR #391 and corrected through PR #392 remains intact and is exercised with its canonical paths, five-state classification, same-interpreter pytest behavior, governed-reference checks, viability checks, and current-state publication semantics. (PF10 — HDE Build Notes, §§2.3, 2.5, 2.9; PF12 — HDE Schemas and Artifacts, §6.2.1.)
- Added deterministic affected-behavior execution for changed test modules and explicitly owned product/evidence-generator surfaces. Tests run from the exact detached head under closed rails with no persisted checkout credentials; fixed-lane coverage is not duplicated, unowned source and generator changes fail closed, and the isolated candidate must remain clean. `adapter/http_reader.py` retains its fixed compatibility, database, and release owners and selects its non-duplicated diagnostic-writer, Reader A7 transport, conjunction, endpoint-catalog, auxiliary-narrative, error-header, import-time database, and public log-route suites. A standalone ownership guard runs for Reader source changes, registered consumers, new absolute static imports, the two reviewed dynamic owners, and the reviewed Reader route-literal owner. The dynamic Reader import tests use one repository-owned seam whose behavioral check proves cache eviction and the exact real-module import, and their reviewed source bytes are explicitly pinned; any new seam import or statically foldable raw Reader reference and every test-tree symlink fail closed rather than silently bypassing registration. The mutable `catalog/manifest.json` source selects its existing release-identity owners without unrelated product, compatibility, or evidence work. (PF10 — HDE Build Notes, §§2.3, 2.5, 2.8.)
- Extended that explicit ownership policy across every tracked Python source in `tools/qa`: approved generic-harness owners reuse the existing QA lane without duplicate execution; bounded EPIC023–EPIC024 helpers and adapters receive focused closed-rails owners; and the source-writing EPIC025/EPIC026 close-pack generators, which have no active executable consumer or focused owner, fail classification pending an explicit reviewed disposition. New unmapped QA tools likewise fail instead of inheriting an unrelated green result. The approved PR #391/#392 fixed QA roster and subsystem behavior remain unchanged. (PF10 — HDE Build Notes, §§2.3, 2.5, 2.8–2.9.)
- Extended affected-behavior ownership across the complete tracked non-generator `tools/evidence` helper inventory: helpers with safe focused or preservation coverage select those exact owners plus an exhaustive ownership guard without duplicating fixed lanes; helpers without a safe current owner fail closed; and paired helper/owner removal remains an explicit reviewed disposition. (PF10 — HDE Build Notes, §§2.3, 2.5, 2.8.)
- Closed the behavioral gap for full-validation inputs with an explicit 34-module configured-suite supplement plus every registered behavioral owner not supplied by an established fixed lane. The derived roster remains disjoint from fixed providers and five inactive administrative or source-writing tests, relies on the workflow's single editable install, uses a canonically resealed temporary narrative pack for current fallback and suppression behavior, and runs deterministically under closed rails; inactive tests require explicit reviewed disposition instead of silently implying current coverage. (PF10 — HDE Build Notes, §§2.3, 2.5, 2.8–2.9.)
- Changed pull-request affected-surface discovery to diff the exact candidate head from the merge base of the event's base and head SHAs, preventing base-only changes from being reversed into the pull-request change set. Default-branch pushes retain the exact `before`-to-`after` comparison; rename and deletion parsing and exact-head execution remain unchanged; and unavailable or shallow merge-base history fails closed. (PF10 — HDE Build Notes, §§2.5, 2.8.)

These PR-04 changes do not claim HDE-EPIC038 completion or closeout, HDE-EPIC039 completion, QA PASS, acceptance, token satisfaction, PF09 movement, OPS or Live QA execution, deployment, release execution, database or migration work, or epic closeout. They do not alter public routes, payload contracts, Human Design mechanics, database state, or release identity.

## PR-05 feedback-free closeout-candidate lifecycle

- Added the closed `epic_close_candidate_source.v1` contract for one three-digit epic identity, one tracked plan, tracked evidence bindings, deterministic report sections, derived output bindings, and the exact closed canonical nonclaim-code set rendered as fixed truthful statements. Custom report headings and body lines are closed to a writer-owned registry of neutral labels and statements, leaving mandatory nonclaim subjects solely to the fixed canonical section. The contract has no run-ID, receipt, hosted-result, current-attestation, later-commit, release-ID, or caller-selected-output-root field; source roles, aliases, full-form feedback identifiers, and known feedback-path classes fail closed, while plan and evidence payloads remain exact opaque pre-host tracked bytes rather than a lexical text policy, so truthful canonical nonclaims, historical causal-input prose, evidence digests, and binary evidence remain admissible. Exact-head ordering prevents a current or later hosted fact from entering the candidate without tracked source mutation, and the canonical nonclaims may truthfully name the excluded categories. (PF09.1 — HDE Build Checklist — Calcination, HDE-CALC003.22; PF06 — Epic Process Guide, §3.5.1; PF12 — HDE Schemas and Artifacts, §6.2.2.)
- Added one epic-agnostic writer/checker with exactly one explicit mode per invocation. It derives `audit/EPIC-###_close_report.md` and `audit/EPIC-###_MANIFEST.json` from the validated identity, admits only exact tracked regular causal inputs at the exact Git HEAD, renders the complete pair twice in memory, and provides manifest-committed, crash-consistent, fail-closed publication with atomic replacement of each individual file: both staged outputs are flushed, the report is atomically installed and its containing directory synchronized first, and the manifest is atomically installed and its containing directory synchronized last as the sole validity commit point. Check mode performs a stable manifest→report→manifest read, requires byte-identical manifest reads and exact report-hash and canonical-binding agreement, and never repairs or writes source. Catchable pre-commit failures retain bounded rollback and cleanup where safe; an uncatchable interruption after report installation may leave an uncommitted mixed generation, failure after manifest replacement without directory-sync proof has an uncertain commit outcome, and both conditions are reported or validated fail closed. Recovery requires a fresh authorized write-mode derivation from the exact source and HEAD, rechecks that exact HEAD and the byte-identical Git index before every residue-cleanup mutation and throughout publication and final verification, and leaves no tool-owned residue; ambiguous or concurrently changed rollback residue is preserved and rejected. During check and write modes every tracked symlink is verified byte-for-byte against the exact candidate-HEAD blob rather than mutable index stat fields. Both modes use anchored no-atime reads for Git control, reference, object, and index data and invoke no Git subprocess. The sole metadata exception is a kernel-managed atime change on an actually inspected tracked mode-`120000` symlink inode caused unavoidably by reading the exact target bytes; the tool never calls a timestamp-setting API or restores atime, and all target bytes, mode, mtime, ctime, path identity, HEAD, index bytes, causal inputs, ledgers, proof artifacts, and unrelated repository state remain protected. Candidate bytes do not bind source-commit identity, receipts or hosted results, release IDs, external-attestation payloads, hosted metadata, wall-clock values, randomness, or environment identity. (PF09.1, HDE-CALC003.22; PF06, §3.5.1; PF12, §6.2.2; PF27 — Plan Templates, terminal-state reachability, execution-surface feasibility, rails consistency, and end-to-end proof requirements.)
- Added the isolated `epic-closeout-validation` workflow behind explicit manual dispatch only. One bounded exact-ref checkout uses read-only contents permission and no persisted credentials; the job runs the generic non-writing stable manifest→report→manifest check and retained read-only evidence checks under closed rails, with clean-tree and exact-head checks before and after. It publishes and consumes no receipt or artifact and writes no tracked bytes, mtime, ctime, branch, commit, proof, or ledger state; only the bounded kernel-managed tracked-symlink atime exception described above may occur during exact target verification. (PF10 — HDE Build Notes, §2.5; PF12, §6.2.2; PF14 — HDE Mechanics Guide, §27.3.)
- Registered the generic QA source and closed schema with their exact behavioral owner while retaining exhaustive, nonoverlapping, fail-closed ownership. The accepted PR #391/#392 generic QA roster and the single ordinary `test` job remain unchanged; unknown QA tools still fail classification. (PF10, §§2.3, 2.5, 2.8–2.9.)
- Added bounded temporary-Git-repository tests for tracked-input generation, exact canonical paths and bytes, report-first and manifest-last installation, staged-file and containing-directory synchronization boundaries, process termination at every material publication and cleanup boundary, concurrent-reader interleavings, report-new/manifest-old and unstable-manifest rejection, same-length and same-mode report substitution rejection, catchable-failure rollback, fresh-write recovery, zero tool-owned residue after successful and recovered executions, candidate commit, clean exact-head check, deterministic second render, non-writing metadata posture, exact tracked-symlink target comparison, checksum-valid poisoned-index refusal in both modes, unchanged-atime and relatime-triggered-atime cases in both modes, byte-identical index preservation, dirty and unsafe input refusal, prohibited feedback fields, and the absence of HDE-EPIC038, ledger, proof, release, network, acceptance, token, or QA-output writer coupling. Interrupted observations are limited to a complete old generation, complete new generation, or a fail-closed uncommitted or unstable state. These tests exercise terminal reachability and prove that only the explicitly bounded symlink atime may differ during check or write mode, without creating governed candidates in the real checkout. (PF09.1, HDE-CALC003.22; PF06, §3.5.1; PF27, terminal-state reachability and end-to-end proof requirements.)
- Preserved evidence ownership: the candidate writer owns only the two future candidate outputs, while the canonical evidence updater remains the sole writer of the Human Evidence Index, Machine Mirror, hash sentinels, and sibling path proofs. This Doc-Delta pair remains the bounded PR-05 implementation record and its existing updater-owned bindings are refreshed through the canonical publication sequence. (PF12, §§6.2.2, 8.3; PF14, §27.3.)

### PF10 §2.5 closeout-event validation matrix

| Event shape | Before PR-05 | After PR-05 |
| :--- | :--- | :--- |
| Ordinary pull request | One exact-head `test` job; no generic closeout lane | Unchanged: one exact-head `test` job; the manual closeout job is not eligible |
| Default-branch `main` update | One `test` job; no generic closeout lane | Unchanged: one `test` job; the manual closeout job is not eligible |
| Explicit closeout validation | No generic lane | One `validate-closeout-candidate` job behind `workflow_dispatch`, validating only the selected exact committed candidate |
| Receipt, artifact, or source feedback | No active closeout receipt-feedback path | None introduced; no transfer, writeback, later-source dependency, or required-check dependency |

The final execution shape preserves PR-04's material reduction: ordinary pull-request and `main` events retain the single existing job and stable conclusion, while closeout-candidate validation exists only as an isolated manual read-only job. No equivalent full suite, overlapping automatic trigger, required-check or repository-setting assumption, receipt transfer, or tracked-source feedback edge is introduced. (PF10, §2.5; PF12, §6.2.2.)

No real HDE-EPIC039 close report, close manifest, acceptance map, token-evidence matrix, QA log, or close candidate is produced by PR-05. These changes do not claim QA PASS, token satisfaction, acceptance, PF09 status movement, Product Owner closeout, HDE-EPIC039 completion, OPS or Live QA execution, deployment, release, migration, or epic closeout; they do not alter public routes, payload contracts, Human Design mechanics, database behavior, release identity, or external-attestation contracts. (PF09.1, HDE-CALC003.22; PF06, §3.5.1; PF12, §6.2.2; PF14, §27.3.)
