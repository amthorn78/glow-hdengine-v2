# HDE-EPIC035 PR-03 Doc-Delta Candidates

Scope: same-PR drift-capture candidates only. PF-Canon was not edited.

- PF29 should clarify that `hdctl bg:resolve --source vendor` is a legacy BodyGraph ingest-path observation and is not the canonical v2 chart/geokey validation path.
- PF29 should clarify that v2 chart/geokey header posture is proven via `charts` or `charts/simple` observation with `Authorization: Bearer <redacted>`, `HD-Geocode-Key: <redacted>`, and legacy `HD-Api-Key` absent.
- PF29 should clarify acceptable OPS nested evidence-root manifest mapping when the retained evidence manifest maps approved deliverable names to retained paths.

Nonclaims: no PF09 status movement, no HDE-FERM008 parent Done, no epic closeout, no QA PASS, no OPS completion, no full HumanDesignAPI v2 runtime conformance, no public Reader change, no public route, no public flag, no public payload or transport change, no new HTTP home, no app-side HumanDesignAPI credential ownership, no raw payload persistence, and no AI scope.
