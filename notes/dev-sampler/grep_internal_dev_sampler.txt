./audit/qa/hde-epic019/discovery-20251130/D0_http_sampler_code_locations.txt:2:command: grep -R "internal/dev/sampler" in repo
./audit/qa/hde-epic019/discovery-20251130/D0_http_sampler_code_locations.txt:4:=== grep -R "internal/dev/sampler" . ===
./audit/qa/hde-epic019/discovery-20251130/D0_http_sampler_code_locations.txt:5:./tests/adapter/test_dev_sampler_http.py:14:        "/internal/dev/sampler",
./audit/qa/hde-epic019/discovery-20251130/D0_http_sampler_code_locations.txt:6:./tools/evidence/generate_sampler_evidence.py:310:            "/internal/dev/sampler",
./audit/qa/hde-epic019/discovery-20251130/D0_http_sampler_code_locations.txt:7:./adapter/http_reader.py:488:    @bp.route("/internal/dev/sampler", methods=["POST"], provide_automatic_options=False)
./audit/qa/hde-epic019/live-qa-20251130/D3-http-dev-sampler-gating.log:2:command: curl -X POST http://127.0.0.1:2000/internal/dev/sampler (APP_ENV=prod, ALLOW_NETWORK=1)
./audit/qa/hde-epic019/live-qa-20251130/D3-http-dev-sampler-allowed.log:2:command: curl -X POST http://127.0.0.1:2000/internal/dev/sampler (APP_ENV=dev, ALLOW_NETWORK=1)
./audit/qa/hde-epic019/live-qa-20251130/D3-http-sampler-rails.log:8:DEV_SAMPLER_URL=http://127.0.0.1:2000/internal/dev/sampler
./audit/qa/hde-epic019/logs/step4_http_sampler_rails.txt:4:url: http://localhost:8000/internal/dev/sampler
./glow_hdengine.egg-info/PKG-INFO:25:LC_ALL=C LANG=C TZ=UTC SAFE_MODE=1 ALLOW_NETWORK=0 python -m adapter.http_reader  # exposes /internal/dev/sampler when APP_ENV=dev
./glow_hdengine.egg-info/PKG-INFO:62:- Reader harness mirrors CLI bytes for the same inputs; APP_ENV gating remains in `engine/http/compat_handler.py`. A dev-only sampler endpoint lives at `/internal/dev/sampler` (APP_ENV=dev) for QA parity with the sampler CLI.
./README.md:16:LC_ALL=C LANG=C TZ=UTC SAFE_MODE=1 ALLOW_NETWORK=0 python -m adapter.http_reader  # exposes /internal/dev/sampler when APP_ENV=dev
./README.md:53:- Reader harness mirrors CLI bytes for the same inputs; APP_ENV gating remains in `engine/http/compat_handler.py`. A dev-only sampler endpoint lives at `/internal/dev/sampler` (APP_ENV=dev) for QA parity with the sampler CLI.
./tests/adapter/test_dev_sampler_http.py:14:        "/internal/dev/sampler",
grep: ./.venv/bin/python3.12: No such file or directory
grep: ./.venv/bin/python3.11: No such file or directory
grep: ./.venv/bin/python3: No such file or directory
grep: ./.venv/bin/python: No such file or directory
./docs/pfcanon/PF19-Canon-Glow QA Guide v1.3.6.md:280:* **Internal/dev HTTP harnesses (e.g. `/internal/dev/sampler`).**  
./docs/pfcanon/PF19-Canon-Glow QA Guide v1.3.6.md:281:   Dev-only HTTP routes exposed by the Reader/adapter for internal QA or evidence flows (for example `POST /internal/dev/sampler` for sampler behavior).
./docs/pfcanon/PF19-Canon-Glow QA Guide v1.3.6.md:294:      * a corresponding base URL and port per environment (for example Codespaces vs local dev), from which concrete URLs such as `DEV_SAMPLER_URL=http://127.0.0.1:<port>/internal/dev/sampler` can be derived.
./docs/pfcanon/PF19-Canon-Glow QA Guide v1.3.6.md:1158:Live QA steps that exercise **HTTP surfaces** (for example internal/dev harnesses such as `/internal/dev/sampler`) can fail for two very different reasons:
./docs/pfcanon/PF19-Canon-Glow QA Guide v1.3.6.md:4195:  * internal/dev HTTP harnesses (for example `/internal/dev/sampler`), including:
./docs/pfcanon/PF05-Canon-HDE-CLI-API-Vendor-Ref v1.4.1.md:1418:  * The dev sampler HTTP harness (`POST /internal/dev/sampler` in §5.11) is a **secondary**, infra-dependent harness:
./docs/pfcanon/PF05-Canon-HDE-CLI-API-Vendor-Ref v1.4.1.md:2187:* Path: `/internal/dev/sampler`.
./docs/pfcanon/PF05-Canon-HDE-CLI-API-Vendor-Ref v1.4.1.md:2285:* For a fixed `viewer_id`, `candidate_ids` array, `seed` value (including `null`), environment pins, and `APP_ENV`, two successive `POST /internal/dev/sampler` calls **MUST** produce byte-identical responses (status, headers, and LF-terminated body).
./docs/pfcanon/PF05-Canon-HDE-CLI-API-Vendor-Ref v1.4.1.md:2301:* `POST /internal/dev/sampler` is a **dev/admin-only internal harness** and is explicitly excluded from the Endpoint Catalog (§5.6). No A7 proofs run on this route.
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1007:* Route: POST /internal/dev/sampler
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1025:* When APP\_ENV is any other value (including prod), or when APP\_ENV is missing or empty, POST /internal/dev/sampler returns a writer-style 403 forbidden response with the standard typed error envelope for refusal (numeric-free, canonical JSON), and does not call the sampler core.
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1077:For `/internal/dev/sampler` and any other internal/dev HTTP harness that is intended for QA or evidence flows and is not part of the public API, Mechanics requires a clear split of responsibilities between infra/ops and QA/PO, and concrete wiring for start commands and URLs.
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1081:* For each environment where `/internal/dev/sampler` is intended to be used (at minimum: local dev and Codespaces), infra/ops **MUST** define and maintain a canonical dev Reader start command or service definition that:
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1099:  * `DEV_SAMPLER_URL = <base_url>/internal/dev/sampler`
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1103:  * QA plans and doc agents **MUST** consume `DEV_SAMPLER_URL` (or an equivalent infra-exposed value) as an input and **MUST NOT** guess hostnames, ports, or full URLs for `/internal/dev/sampler`.
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1107:Mechanics does not pin where `DEV_SAMPLER_URL` is stored (for example, env var, config file, or Codespaces devcontainer config), only that it exists as a single infra-owned binding for `/internal/dev/sampler` per environment and that QA/PO treat it as the authority for that harness URL.
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1125:  * matches the request/response shape for `/internal/dev/sampler` described in §5.8 (top-level keys, types, and determinism requirements), and
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1135:  * defining and maintaining the dev Reader start commands for internal/dev harnesses (including `/internal/dev/sampler`) per environment,
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1153:* POST /internal/dev/sampler is an internal/dev surface and is explicitly **not** a JSON success route in the Endpoint Catalog.
./docs/pfcanon/PF14-Canon-HDE-Mechanics Guide v2.1.5.md:1863:* CLI and HTTP sampler harnesses in this repo (including the dev/admin CLI sampler and the `/internal/dev/sampler` HTTP harness) **MUST** call these canonical helpers rather than reimplementing sampling or ranking logic locally. Mechanics treats `engine.sampler.core` as the **single home** for sampler/ranker behavior; harnesses add only I/O, env wiring, and evidence capture.
./docs/pfcanon/PF07-Canon-Glow-Infrastructure v1.1.7.md:367:For internal/dev HTTP harnesses on the HD Engine (including but not limited to `POST /internal/dev/sampler`), Codespaces and other local dev environments are expected to provide:
./docs/pfcanon/PF07-Canon-Glow-Infrastructure v1.1.7.md:369:* A **running dev Reader HTTP service** in APP\_ENV=dev that exposes the internal/dev routes defined in the Engine adapter (for example, `/internal/dev/sampler`), reachable from the Codespaces shell.
./docs/pfcanon/PF07-Canon-Glow-Infrastructure v1.1.7.md:891: *Names-only key for the dev-only sampler HTTP harness base URL (for routes such as `POST /internal/dev/sampler`) in dev/Codespaces/local-dev environments. The concrete value (host and port) is **infra-owned** and MUST be derived from the actual Reader process wiring (ports/host binding) for that environment, not guessed by PO, QA, or documentation agents. PF07 records the key name only; behavior, validation steps (for example, HTTP/1.1 JSON POST checks), and header/body contracts are specified by title in **HDE-CLI-API-Vendor-Ref**, **HDE-Mechanics Guide**, and **Glow QA Guide**.*
./docs/pfcanon/PF20-Canon-HDE-Phased Epics v1.2.8.md:2089:   As of PR 4 for HDE-EPIC019, this deliverable is realized by a dev/admin-only HTTP sampler harness at `POST /internal/dev/sampler` on the internal reader surface. The handler uses the same pure-compute sampler core as Deliverables D1/D2, building `ViewerProfile` and `CandidateFeatures` from the incoming candidate IDs and calling the sampler’s `sample_and_rank` function without changing eligibility or ordering semantics. It emits canonical JSON (UTF-8, sorted keys, compact, single trailing newline) containing only `viewer_id`, `meta.seed`, and the ordered `candidate_ids` list, and is **strictly gated by `APP_ENV`**: requests are permitted only when `APP_ENV` is explicitly one of `dev`, `test`, or `local`; missing, empty, or any other `APP_ENV` value produce a writer-style `403 forbidden` envelope. The HTTP harness mirrors the dev sampler CLI semantics (seed is echoed as metadata but does not alter ranking in this PR) and remains an internal dev/admin tool: it is excluded from the Endpoint Catalog and A7 proofs, and detailed HTTP bytes and rails semantics are single-homed in the CLI/API reference, infrastructure, governance, and QA guide documents (titles-only).
./docs/pfcanon/PF20-Canon-HDE-Phased Epics v1.2.8.md:2091:   For **live HTTP QA runs** (for example Live QA steps that call `/internal/dev/sampler` from a Codespaces shell), D3 acceptance is contingent on a working dev Reader HTTP harness in the target environment, at the canonical host/port defined in the infrastructure canon. When QA rails are correctly set (for example `SAFE_MODE=1`, `ALLOW_NETWORK=1`, `APP_ENV=dev` for the allowed call and `APP_ENV=prod` for the gated call) and the request payload matches the handler’s expected JSON shape but the HTTP call fails at the protocol layer (for example `HTTP_STATUS:000`, HTTP/0.9 errors, or otherwise no HTTP/1.x status and no JSON body), the result MUST be classified as an infra/tooling failure (for example `FAIL_TOOLING` in QA logs) rather than as a sampler behavior pass/fail: the dev sampler handler has not been invoked, and D3 behavior and gating remain unproven. Under those conditions, D3 cannot be marked satisfied or Green for this epic; it remains **blocked by infra** until:
./docs/pfcanon/PF20-Canon-HDE-Phased Epics v1.2.8.md:2095:  * QA can successfully exercise `/internal/dev/sampler` under both APP\_ENV=dev (expected canonical JSON 200\) and APP\_ENV=prod (expected writer-style 403 forbidden envelope), with the resulting logs and artifacts wired into the acceptance map and manifest for this epic.
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:2626:**EPIC019 PR04 (D3 — “Dev-only sampler HTTP endpoint harness”)** adds a dev/admin-only HTTP sampler harness at `POST /internal/dev/sampler` (implemented in `adapter/http_reader.py` with tests in `tests/adapter/test_dev_sampler_http.py`):
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:2628:Registers `POST /internal/dev/sampler` as an internal, non-public route.
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:2921:`adapter/http_reader.py` — registers and implements `POST /internal/dev/sampler` as a dev/admin-only sampler harness that:
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:2945:`tests/adapter/test_dev_sampler_http.py::test_dev_sampler_determinism` — under `APP_ENV=dev` and closed rails, posts the same valid payload to `POST /internal/dev/sampler` twice and asserts:
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:2961:`tests/adapter/test_dev_sampler_http.py::test_dev_sampler_rejected_in_prod` — sets `APP_ENV="prod"` and asserts that `POST /internal/dev/sampler` returns 403 forbidden with a writer-style forbidden envelope (no body data leakage).
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:2971:The `POST /internal/dev/sampler` endpoint wraps the pure-compute sampler core and provides an HTTP interface that mirrors the dev sampler CLI semantics: IDs-only payload, `meta.seed` echo, deterministic ordering, and seed-independent ranking.
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:4555: Ensure that any **internal/dev HTTP harness** intended for QA or evidence flows (including, but not limited to, `POST /internal/dev/sampler`) has infra-owned start commands and URLs, so QA and PO are never guessing hosts, ports, or paths:
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:4563:   `DEV_SAMPLER_URL = <base_url>/internal/dev/sampler`
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:4567:  * QA plans and documentation agents MUST consume `DEV_SAMPLER_URL` (or an equivalent infra-exposed binding) as an input and MUST NOT guess hostnames, ports, or full URLs for `/internal/dev/sampler`.
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:4571:  * Mechanics does not pin where `DEV_SAMPLER_URL` lives (env var, config file, devcontainer config); it requires that there is a **single infra-owned binding** for `/internal/dev/sampler` per environment and that QA/PO treat it as the authority for that harness URL.
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:4588:    * matches the `POST /internal/dev/sampler` request/response shape defined in CLI/API and Mechanics by title, and
./docs/pfcanon/PF09-Canon-HDE-Build Checklist v2.5.3.md:6129:  * For internal/dev HTTP harnesses (such as `/internal/dev/sampler`), the canonical dev Reader start command and the base URL/port in this environment, preferably leveraging infra-owned bindings such as `DEV_SAMPLER_URL` defined under HDE-CONJ001.4 (titles-only).
./docs/INDEX.md:24:- Dev sampler endpoint: `adapter/http_reader.py::/internal/dev/sampler` (APP_ENV=dev)
grep: ./notes/dev-sampler/grep_internal_dev_sampler.txt: input file is also the output
./tools/evidence/generate_sampler_evidence.py:310:            "/internal/dev/sampler",
./adapter/http_reader.py:488:    @bp.route("/internal/dev/sampler", methods=["POST"], provide_automatic_options=False)
./CHANGELOG.md:6:- Sampler core (HDE-DISS003) completed with deterministic sampler harnesses (dev-only CLI `hdctl dev:sampler` and HTTP `/internal/dev/sampler`), seeded replay logs, diversity proofs, and governed evidence families indexed for sampler pool snapshots, ABBA parity, two-run identity, and seed replay.
