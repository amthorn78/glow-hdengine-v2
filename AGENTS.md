# AGENTS.md — Glow HD Engine (agent rules)

## Scope and hierarchy
- This file governs all agents in the repo; PF-Canon remains the source of truth (see `docs/pfcanon/`, titles such as PF05 — CLI/API/Vendor Ref, PF10 — HDE-Build Notes (precedence where PF10 speaks), PF12 — Schemas & Artifacts, PF14 — Mechanics Guide, PF19 — QA Guide, PF20 — Phased Epics). Where this file and PF-Canon diverge, PF-Canon wins, with PF10 precedence applying wherever PF10 speaks.
- Governed evidence (INDEX/mirror/path proofs/orientation/manifest/close report/config acceptance map) must be produced only by the canonical tools. **Never hand-edit governed artifacts.**
- `docs/pfcanon/**` is read-only for Codex/dev agents; cite PF canon by title/§ only.

## Agent roster (repo-facing)
- **Lead Dev / Product Owner:** approves epic scopes and evidence plans; owns acceptance maps/manifests and public/ops surface sign-off. Touches docs and governance bindings (acceptance maps, manifests, close reports).
- **Codex / dev agents:** implement features/docs/evidence under PO direction; run CLI guards and harnesses under closed rails; maintain the dev Reader helper (`scripts/dev_start_reader.sh`) and DEV_SAMPLER_URL wiring (dev/test/local only). Touch code, docs, and governed evidence generators.
- **Evidence harness:** runs `tools/generate_registry_report.py`, `tools/evidence/update_evidence_index.py`, `tools/evidence/orientation_demo.py`, `tools/evidence/run_sanity_pipeline.py`, `tools/evidence/validate_evidence_paths.py`, `tools/evidence/check_lf_endings.py`, epic-specific generators (including `tools/cli/generate_showcompat_artifacts.py` for EPIC022 D2, `tools/evidence/generate_conjunction_writer_evidence.py`, and `tools/qa/generate_epic027_close_pack.py`), and keeps `docs/evidence/INDEX.json` and `artifacts/evidence_index.jsonl` aligned with `.path_proof.txt` siblings.
- **Config/bundle agents:** run `tools/config/generate_config_artifacts.py` and `tools/config/generate_bundles.py`; confirm `audit/EPIC-018_config_acceptance_map.json` stays consistent with generated artifacts and path proofs.
- **QA/Verifier:** executes the sanity pipeline, env-pins gate, EPIC020 deterministic suites (error envelope, presenter, `/internal/version`), the registry report generator, EPIC021 QA harness (`tools/qa/epic021_qa.py`), EPIC024 QA harness (`tools/qa/run_hde_epic024_harness.py`), EPIC025 preflight + gate logs under `audit/qa/hde-epic025/`, EPIC027 gate logs under `audit/qa/hde-epic027/`, EPIC030 PR evidence families under `audit/qa/hde-epic030/pr-01/` … `pr-05/`, EPIC031 SAFE rails PR evidence families under `audit/qa/hde-epic031/pr-01/` … `pr-03/`, and EPIC022 acceptance scaffolding. Confirms tokens and evidence coverage in manifests/acceptance maps and updates Index/Mirror.
- **Doc agents:** refresh README/CHANGELOG/AGENTS/docs to reflect PF-Canon titles and epic outcomes; include rails/guardrails for determinism and evidence, dev vs public surfaces, and closed-rails posture for EPIC020–EPIC022, and the completed EPIC031 SAFE rails docs posture.

## Operating workflow (closed rails, evidence discipline)
- Closed rails default: `LC_ALL=C`, `LANG=C`, `TZ=UTC`, `SAFE_MODE=1`, `ALLOW_NETWORK=0` enforced by `engine.runtime.determinism_env.ensure_determinism_env` and checked via `ci/checks/check_env_pins.sh`. No network access for public/QA surfaces unless explicitly allowed for a governed harness.
- Pytest readiness rule: always install dev test dependencies before pytest-dependent checks using `python -m pip install -r requirements-dev.txt` (or the environment-equivalent Python executable), then run `python -m pytest --version` as readiness proof.
- PF05 showcompat posture: when Live QA requires `hdctl showcompat` in pre-Glow environments, use birth-argument inputs with `--source vendor` and allow open rails as required by the plan; record the active env values in the step log. Treat zero-arg showcompat plans as planning defects and apply a minimal Moon Loop deviation to align with PF05.
- Conjunction surfaces (EPIC027): use `hdctl showcompat --conjunction` for CLI conjunction output. Input modes are verified in-repo as `--user-a/--user-b`, `--pair-file`, `--a-file/--b-file`, or stdin pair payloads; do not invent `--pair` syntax.
- Conjunction SAFE rails: unresolved conjunction users resolve through BodyGraph lookup/resolver. Default rails are closed (`SAFE_MODE=1`, `ALLOW_NETWORK=0`) so provider acquisition can refuse explicitly (`PROVIDER_REFUSED`); open rails require both `SAFE_MODE=0` and `ALLOW_NETWORK=1`.
- Dev-only conjunction HTTP routes: `/dev/sampler/conjunction`, `/dev/reader/conjunction`, `/dev/writer/conjunction` are APP_ENV-gated to `dev|test|local` and are cataloged in `docs/ENDPOINTS_CATALOG.json` (docs catalog is source-of-truth; audit mirror at `artifacts/audit/ENDPOINTS_CATALOG.json`).
- Redaction rule: QA step logs MUST NOT include secrets. When capturing env probes (APP_ENV, ALLOW_NETWORK, GEO_API_KEY, HDAPI_BASE_URL, HD_API_KEY, SAFE_MODE), redact sensitive values (keys, tokens, base URLs) and record only “REDACTED” or “SET/UNSET”.
- Read-first, then edit: inspect acceptance bindings, evidence indexes, and QA harness expectations before changing docs/code/evidence.
- QA output placement: **do not create QA artifacts in the repo root**. Write QA outputs only under the active evidence directory (e.g., `audit/qa/<epic-id>/...`) unless a PF-canon/governed tool explicitly specifies otherwise.
- Docs-only PR posture: do not make behavioral claims without proof. Evidence must come from repo-governed artifacts, enforced tests/CI steps, or PF canon references (titles/§ only). Keep statements scoped to current repo state.
- Governed evidence rules:
  - Use only repo tools to regenerate governed artifacts. Examples: CLI guards (`python tools/cli/serializer_grep_guard.py`, `python tools/cli/emitter_symbol_proof.py`), registry report (`python tools/generate_registry_report.py`), showcompat D2 capture (`python tools/cli/generate_showcompat_artifacts.py`), orientation demo (`python tools/evidence/orientation_demo.py`), sanity pipeline (`python tools/evidence/run_sanity_pipeline.py`), Evidence Index updater (`python tools/evidence/update_evidence_index.py`), mirror schema check (`ci/checks/check_mirror_schema.sh`).
  - Refresh Index/Mirror pairs (`docs/evidence/INDEX.json`, `docs/evidence/INDEX.sha256`, `artifacts/evidence_index.jsonl`) and confirm `.path_proof.txt` siblings exist for every governed artifact. Run `update_evidence_index.py` (write) before `orientation_demo.py` (write), then their `--check` variants, and finish with the mirror schema check. Mirror path proofs include `mirror_body_sha256` for the self-record; do not hand-edit or drop the self-record row.
  - Canonical JSON gate home is `audit/gates/json_gate/canonical/` (`json_gate_check_log.ndjson`, `json_gate_compare_log.ndjson`, `json_gate_structured_record.json` + `.path_proof.txt` siblings) produced by `python tools/evidence/run_canonical_json_gate.py` (CI step: “Run canonical JSON gate (closed rails)”). The legacy catalog check report remains at `audit/gates/canonical_json/json_canonical_check.log`.
  - Evidence index snapshot gate family lives at `audit/gates/evidence_index_snapshot/evidence_index_snapshot.json` with its `.path_proof.txt` sibling, produced by `python tools/evidence/generate_evidence_index_snapshot.py`.
  - EPIC027 QA evidence root: `audit/qa/hde-epic027/` with per-check logs under `audit/qa/hde-epic027/checks/<check_id>/primary.log`, token matrix at `audit/qa/hde-epic027/token_evidence_matrix.md`, and viability gate log at `audit/qa/hde-epic027/acceptance_map_viability.log`. Do not place QA logs outside the epic root.
  - EPIC027 evidence gates: `python tools/evidence/validate_evidence_paths.py` (evidence path validation) and `python tools/evidence/check_lf_endings.py` (final-LF discipline) should log under `audit/qa/hde-epic027/checks/gate_evidence_paths_validation/` and `audit/qa/hde-epic027/checks/gate_lf_endings/`.
  - Showcompat stdout determinism: `hdctl showcompat` emits canonical bytes via the shared presenter/emitter and must end with exactly one LF; CRLF is rejected by `STDOUT_MISSING_LF` / `STDOUT_CRLF` guards in `engine/cli/main.py`. Deterministic captures are generated by `python tools/cli/generate_showcompat_artifacts.py` under `artifacts/cli/showcompat/`.
  - EPIC027 close pack: `python tools/qa/generate_epic027_close_pack.py` writes `audit/EPIC-027_MANIFEST.json` and `audit/EPIC-027_close_report.md`; acceptance ledger artifacts are `docs/acceptance_map_epic027.json`, `audit/qa/hde-epic027/token_evidence_matrix.md`, and `audit/qa/hde-epic027/acceptance_map_viability.log`.
  - Verify acceptance bindings: EPIC018 manifest/close report under `audit/`; EPIC019/EPIC020/EPIC021/EPIC022/EPIC023/EPIC024 acceptance maps under `docs/`; token matrices under `audit/qa/hde-epic021/`, `audit/qa/hde-epic022/`, `audit/qa/hde-epic023/`, and `audit/qa/hde-epic024/`.
  - EPIC023 acceptance discipline: governed acceptance artifacts live in `docs/acceptance_map_epic023.json` and `audit/qa/hde-epic023/` (token matrix, viability log, QA step manifest, doc deltas, PF23 consult). The canonical token roster is `{QA_ACCEPTANCE_MAP_VIABILITY_OK, EVIDENCE_INDEX_MIRROR_OK, EVIDENCE_PATHS_VALIDATED_OK, SANITY_PIPELINE_OK, DETERMINISM_ENV_PINS_OK, JSON_CANONICAL_CHECK_OK, DOC_DELTA_PRESENT_OK, TWO_RUN_IDENTITY_OK}`; keep acceptance map, matrix, and Index/Mirror entries in lockstep and never treat `.path_proof.txt` as primary evidence. Close-pack artifacts are governed at `audit/EPIC-023_MANIFEST.json` and `audit/EPIC-023_close_report.md`.
  - EPIC024 acceptance discipline: governed acceptance artifacts live in `docs/acceptance_map_epic024.json` and `audit/qa/hde-epic024/` (token matrix, viability log, QA step manifest, per-check logs, doc deltas). Acceptance-map viability is a PASS/FAIL_BEHAVIOR gate that affects the EPIC024 harness exit status. Close-pack artifacts are governed at `audit/EPIC-024_MANIFEST.json` and `audit/EPIC-024_close_report.md`.
- **No manual edits** to evidence indexes, path proofs, manifests, close reports, acceptance maps, or orientation artifacts.

## Release identity (EPIC022 Remediation 1; PF10 precedence where it speaks)
- Single Freeze-Pack SoT: checked-in `catalog/manifest.json` with top-level keys exactly `{root,version,built_at_utc,files}` (no self-listing). Canonical bytes rule applies (UTF-8, ASCII-sorted keys, compact separators, one trailing `\n`); `release_id = sha256(canonical_bytes(catalog/manifest.json))`.
- Evidence-copy semantics: `artifacts/math/freeze_pack_manifest.json` must be byte-identical to `catalog/manifest.json`; `manifest_snapshot.json` and other summaries are evidence-only and not identity inputs. No alternate artifacts may reuse `artifacts/math/freeze_pack_manifest.json`.
- Governed evidence set (must exist, non-empty): `artifacts/math/release_id.txt`, `artifacts/math/release_id_recompute.log`, `artifacts/math/checksums_audit.log`, `artifacts/math/manifest_snapshot.json`, `artifacts/proofs/env_pins.txt`. Do not hand-edit these artifacts or their path proofs.
- Closed-rails validation commands (Python entrypoints in CI): `python scripts/release_id_recompute.py --check` (fail-closed recompute), `python ci/checks/check_release_identity.sh` (identity gate), and `python tools/evidence/run_sanity_pipeline.py` (runs the gate alongside other deterministic checks). `--check` writes the recompute log and sha sidecar; run in a clean workspace or discard local changes if invoking locally.


## EPIC031 docs posture (SAFE rails Fermentation Pass 2)
- Treat HDE-EPIC031 as a documentation-visible SAFE rails evidence slice for PF09.5 HDE-FERM001.2/.3/.4, not as PF09.5 drainage or HDAPI v2 runtime completion unless a repo-resident PF09.5 update proves that status. PF10 can supply wording context, but concrete repo claims must be verified against checked-in paths, scripts, tests, workflows, and evidence rows before documentation changes.
- Keep EPIC031 scope boundaries explicit in docs: no public Reader contract change, no public route, no public flag, no public payload field, no live vendor call, no HDAPI v2 runtime conformance, no open-rails v2 smoke runbook, no close-pack output, no token-matrix rows, and no PF-Canon edit.
- EPIC031 PR evidence homes are distinct: PR-01 provider-gate policy under `audit/qa/hde-epic031/pr-01/` plus `artifacts/vendor/`; PR-02 vendor keys-only/redaction evidence under `audit/qa/hde-epic031/pr-02/`; PR-03 evidence/index coherence under `audit/qa/hde-epic031/pr-03/`. Do not move or describe PR-02 vendor-specific evidence as shared DB-bridge artifacts (`artifacts/logs/keys_only.sample.jsonl`, `artifacts/ops/rails_open_scope.txt`).
- EPIC031 validation references must use repo-supported entrypoints only: `ci/jobs/rails_closed_refusal.yml`, `ci/jobs/rails_open_conformance.yml`, `ci/jobs/logs_keys_only_redaction.yml`, `tools/evidence/generate_epic031_pr02_log_posture.py --check`, `tools/evidence/generate_epic031_pr03_evidence_coherence.py --check`, `tools/evidence/update_evidence_index.py --check`, `tools/evidence/orientation_demo.py --check`, `tools/evidence/validate_evidence_paths.py`, `ci/checks/check_mirror_schema.sh`, `ci/checks/check_evidence_index_hash.sh`, and `tools/evidence/check_lf_endings.py`.

## EPIC036 docs posture (Fermentation Pass 7)
- Treat HDE-EPIC036 as a docs-visible route-policy and evidence-loop slice for PF09.5 HDE-FERM008.6 supportability, not as HDE-FERM008 parent completion, PF09.5 status movement, epic closeout, QA PASS, OPS completion, live vendor observation, or full HumanDesignAPI v2 runtime conformance.
- Preserve route-policy truth: `hdctl bg:resolve --source vendor` classifies configured v2 bases as `unsupported_runtime_nonclaim` before legacy `bodygraphs` request construction; non-v2 configured bases preserve explicit legacy BodyGraph fallback. Closed-rails refusal remains earlier than route-policy logic.
- EPIC036 evidence anchors include `artifacts/vendor/hdapi_v2/bg_resolve_route_policy.snapshot.json`, `bg_resolve_bodygraph_detail_proof.json`, `bg_resolve_runtime_nonclaims.json`, `bg_resolve_request_shape.snapshot.json`, `bg_resolve_policy_binding.snapshot.json`, `audit/qa/hde-epic036/route_policy_decision.log`, `docs/acceptance_map_epic036.json`, `audit/qa/hde-epic036/token_evidence_matrix.md`, `audit/qa/hde-epic036/acceptance_map_viability.log`, `audit/docdeltas/hde-epic036_doc_deltas.md`, and `audit/qa/hde-epic036/00_meta/doc_deltas.md`. Do not hand-edit these governed artifacts, Index/Mirror rows, hash sentinels, or path proofs.
- Preserve nonclaims: no public Reader change, public route/flag/payload/transport change, new HTTP home, app-side HumanDesignAPI credential ownership, raw payload persistence, vendor-v2-specific acceptance token, or AI scope. PF-Canon follow-up homes for HDE-FERM008.6 drainage, PF05 route-policy wording, and PF12 `bg_resolve_*` catalog prose remain out of scope for docs agents.

## EPIC035 docs posture (Fermentation Pass 6)
- Treat HDE-EPIC035 as a docs-visible evidence slice completing the remaining HumanDesignAPI v2 live-conformance evidence sequence for HDE-FERM008.3, HDE-FERM008.4, and HDE-FERM008.5 as governed evidence work, not public product expansion or full runtime-conformance completion.
- Preserve PR boundaries: PR-01 is deterministic closed-rails provider-outcome evidence (`artifacts/vendor/hdapi_v2/error_mapping.snapshot.json`, `rate_limit_headers.snapshot.json`); PR-02 is response-normalization evidence that records the exact v2 ChartResult / ChartSimpleResult schema-adapter gap (`response_mapping.snapshot.json`, `release_binding.snapshot.json`); PR-03 binds PR-01, PR-02, and retained OPS-01 evidence through `docs/acceptance_map_epic035.json`, `audit/qa/hde-epic035/token_evidence_matrix.md`, `audit/qa/hde-epic035/ops-01/ops_evidence_binding.log`, Index/Mirror rows, hash sentinels, and path proofs.
- OPS-01 evidence under `audit/ops/hde-epic035/ops-01/` is retained PO-run open-rails evidence contribution only. It proves v2 `charts/simple` used `Authorization: Bearer <redacted>` plus `HD-Geocode-Key: <redacted>` and did not use legacy `HD-Api-Key` on that v2 path; it also records `bg:resolve` as a legacy BodyGraph ingest-path runtime gap returning `PROVIDER_NOT_FOUND` / 404 against the configured v2 base.
- Do not document `hdctl bg:resolve --source vendor` as the canonical v2 chart/geokey validation path; v2 chart/geokey validation belongs to `charts` or `charts/simple` observations. Preserve nonclaims: no public Reader change, public route/flag/payload/transport change, new HTTP home, app-side HumanDesignAPI call path, raw secrets, raw request/response/vendor payload persistence, vendor-v2-specific acceptance token, QA PASS, OPS completion, PF09 status movement, HDE-FERM008 parent Done, epic closeout, full HumanDesignAPI v2 runtime conformance, or AI scope.
- PF29 HD Engine User Guide drainage is a PF-Canon follow-up target seeded by PF10; repo-doc agents may add non-canonical docs guidance only under existing non-pfcanon docs homes, and must not edit `docs/pfcanon/**` or claim PF29 has been drained.

## EPIC034 docs posture (Fermentation Pass 5)
- Treat HDE-EPIC034 as a docs-visible evidence slice for HumanDesignAPI source selection / v1 legacy isolation, request shaping, response-envelope mapping, adapter/presenter boundary proof, closed-rails deterministic shaping/refusal proof, and PR-06 binding of OPS-02 smoke evidence. Do not describe it as HDE-FERM008 parent completion, HDE-FERM008.3/.4/.5 completion, full HumanDesignAPI v2 runtime conformance, public Reader change, public route/flag/payload/transport change, new HTTP home, OPS execution by docs agents, or AI scope.
- Keep vendor version ownership explicit: `HD_API_BASE_URL` owns the vendor API version path; runtime resource paths stay version-neutral (`charts`, `charts/simple`, `charts/coordinates`, and preserved legacy `bodygraphs` / `bodygraphs/simple`). `HDAPI_BASE_URL` is compatibility-only when `HD_API_BASE_URL` is absent, and conflicting values fail closed.
- Keep auth posture explicit and contract-driven: v2 chart-style routes use `Authorization: Bearer <redacted>`; preserved legacy BodyGraph-style routes use `HD-Api-Key: <redacted>`. Do not infer auth selection from `/v1` or `/v2` string inspection or hardcode `/v2` into runtime route constants/examples/runbooks.
- EPIC034 evidence anchors include `artifacts/vendor/hdapi_v2/source_selection.snapshot.json`, `request_shaping.snapshot.json`, `response_mapping.snapshot.json`, `adapter_boundary_proof.log`, `closed_rails_refusal.txt`, QA logs under `audit/qa/hde-epic034/pr-01/` … `pr-06/`, OPS-02 retained evidence under `audit/ops/hde-epic034/ops-02/`, `docs/acceptance_map_epic034.json`, and doc deltas under `audit/docdeltas/hde-epic034_doc_deltas.md` / `audit/qa/hde-epic034/00_meta/doc_deltas.md`. Treat `artifacts/vendor/hdapi_v2/` as a governed evidence-family name, not runtime API-version authority.
- OPS-02 evidence is bounded PO-authorized open-rails HumanDesignAPI v2 smoke evidence for `charts/coordinates` and HDE-FERM008.2 support only. PR-06 binds that already-produced evidence under closed rails; docs agents must not rerun live vendor calls or widen the claim.

## EPIC033 docs posture (Fermentation Pass 4 / PR-01)
- Treat HDE-EPIC033 PR-01 as a documentation-visible HumanDesignAPI v2 and legacy v1 contract-inventory evidence slice for HDE-FERM006.1 through HDE-FERM006.4. Do not describe it as runtime v2 adapter conformance, request shaping, source selection, live conformance, public Reader change, open-rails vendor smoke, new HTTP home, or AI scope; HDE-FERM007 and HDE-FERM008 remain follow-up/out of scope unless later repo evidence proves otherwise.
- EPIC033 evidence anchors are `artifacts/vendor/hdapi_v2/` (`source_inventory.json`, `source_inventory.md`, `openapi_validation.log`, `known_anomalies.md`, `endpoint_reference.csv`, `contract_map.json`, plus `.path_proof.txt` siblings), source-cache inputs under `artifacts/vendor/hdapi_v2/source_cache/`, `docs/acceptance_map_epic033.json`, `audit/qa/hde-epic033/token_evidence_matrix.md`, `audit/qa/hde-epic033/acceptance_map_viability.log`, and doc deltas under `audit/docdeltas/hde-epic033_doc_deltas.md` / `audit/qa/hde-epic033/00_meta/doc_deltas.md`. Index/Mirror coverage remains `docs/evidence/INDEX.json` and `artifacts/evidence_index.jsonl`.
- Use `tools/evidence/generate_hdapi_v2_contract_inventory.py` for governed EPIC033 inventory outputs. Default generation is closed-rails source-cache replay; public documentation refresh requires explicit `--refresh-public-docs` plus `SAFE_MODE=0` and `ALLOW_NETWORK=1`, remains secret-free, and is not runtime vendor smoke.
- Treat `llms.txt`, `llms-full.txt`, and AI/LLM-oriented vendor documentation as documentation-discovery-only context: no AI product, runtime, evidence, token, credential, rail, QA, prompt, embedding, chatbot, model-call, or provider scope is created. Do not add vendor-v2-specific acceptance tokens.
- Do not document `api-reference/openapi.json` as trusted authority unless repo evidence changes. Current EPIC033 inventory quarantines it and promotes validated `v2-routes.yaml` and `v1-routes.yaml` source-cache specs as route-source authority for the inventory slice.
- Review raw repo/source identity before treating backslashes in rendered assistant or Markdown output as defects; rendered escape artifacts are not source defects when the underlying command, path, artifact, environment-variable, or proof identity is clear.

## EPIC032 docs posture (Fermentation Pass 3)
- Treat HDE-EPIC032 as a docs-visible Fermentation Pass 3 evidence slice for narrative router parity/indexing, narrative registry diff/identity/indexing, DB bridge/provider parity posture, non-dev typed DB failure posture, OPS-01 provider parity closure evidence, and index/mirror/path-proof coherence.
- Keep narrative scope explicit in repo docs: keys-only router/registry evidence (`audit/gates/narratives/keys_10x4.table.json`, `artifacts/narratives/router/parity_abba.log`, `artifacts/narratives/router/cli_http_parity.log`, `audit/gates/narratives/registry.diff.json`, `audit/gates/narratives/pack_identity.txt`) without claiming narrative prose-generation delivery.
- Keep DB evidence scope explicit: `tools/evidence/generate_db_bridge_parity.py`, `artifacts/db_bridge/provider_parity.proof.json`, `artifacts/runtime/env_connectivity.nondev_failure.json`, and `audit/ops/hde-epic032/db-provider-parity/provider_parity_closure_decision.json` are repo evidence anchors; OPS-01 remains OPS evidence (not QA PASS, not PF09 status move, not epic close claim).
- Proof-label posture for this epic: `DB_PROVIDER_PARITY_OK`, `DB_BRIDGE_CAPS_OK`, and `DB_BRIDGE_FALLBACK_OK` are proof labels unless minted later; `DEV_DB_BRIDGE_FALLBACK_OK` remains the acceptance token when scope is dev bridge fallback.
- Preserve public-surface boundaries: no public Reader contract/route/field/flag changes and no HDAPI v2 conformance claims from EPIC032 docs updates.

## EPIC030 docs posture (Dissolution Pass 3)
- Treat HDE-DISS005.2/.3/.4 and HDE-DISS006.3/.4/.5 as active EPIC030 Dissolution scope in repo docs; do not label them deferred or historical-only when EPIC030 governed evidence exists.
- Treat HDE-DISS005.1 and HDE-DISS006.1/.2 as reused prior foundations in EPIC030 docs updates; do not describe them as newly implemented by docs-only sweeps.
- Keep public/admin boundaries explicit: public Reader remains bands-only and numeric-free, while admin/test compat and dev sampler evidence can include category details, scores, or seed metadata on non-public surfaces.
- EPIC030 implementation-slice evidence lives under `audit/qa/hde-epic030/pr-01/` … `pr-05/`; do not describe these PR slices as EPIC close-pack outputs unless separate close artifacts are generated.

## EPIC022 lessons (anti-drift)
- Deterministic scenarios first: parity/error envelopes and showcompat captures must run under pinned rails (no “environment roulette”) with stdout/stderr separation proven by tests and governed artifacts.
- Acceptance artifacts stay single-source: token matrices and acceptance maps must bind to concrete evidence/tests (no duplicate rows or placeholder evidence once concrete artifacts exist); showcompat D2 captures stay indexed with sha256 sidecars and path proofs; `/internal/version` D3 bundles and the close-pack (manifest + close report + sanity log) are present and must not be claimed as missing. PF05 exit-code taxonomy remains canonical even while repo tests pin showcompat vendor/engine errors to exit 1; PF20 D3 references a provenance note while implementation follows the PF10 posture (governed two-run/coupling log) — document the mismatch when relevant.
